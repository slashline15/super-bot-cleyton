# def test_roundtrip():
#     mm = MemoryManager("./data/chroma_db")
#     mm.add_message_sync(1, 42, "ping", role="user")
#     ctx = mm.search_messages(1, 42, "ping")
#     assert any("ping" in d['content'] for d in ctx)

