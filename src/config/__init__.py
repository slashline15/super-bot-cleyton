"""
Configuração do projeto.

Este módulo define a configuração do projeto, incluindo variáveis de ambiente,
configurações de banco de dados e outras configurações necessárias para o funcionamento

Attributes:
    Config: Classe de configuração do projeto

Example:
    >>> from config import Config
    >>> config = Config()
"""

from .config import Config

__all__ = ['Config']
